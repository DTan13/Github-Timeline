import React from 'react';
import Layout from '../../components/layout';

const Home = props => {
    return (
        <Layout>
            <div className="home-page-wrapper">
                Home Page Works
            </div>
        </Layout>
    );
};

export default Home;